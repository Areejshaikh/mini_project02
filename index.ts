// Q3 Write a program to determine the greater of two numbers.


let num1 : number = 99;
let num2 : number = 59;

if (num1 >= num2) {
    console.log(`${num1} is greater then to ${num2}! `);
} else {
    console.log(`${num1} is Less then to ${num2} !`);
}